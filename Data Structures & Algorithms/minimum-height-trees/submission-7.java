class Solution {
    public List<Integer> findMinHeightTrees(int n, int[][] edges) {
        if (n == 1) {
            var res = Arrays.asList(new int[]{0});
        }
        List<Integer>[] adjList = new List[n];
        for (int i = 0; i < n; i++) {

        }
        return new ArrayList<>();
    }
}